// Root build file. Versions are a known-good combo; Android Studio's AGP
// Upgrade Assistant can bump them if your installed SDK/Gradle differ.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
